{
  "name": "pm2-health-check",
  "version": "1.0.0",
  "description": "a health check endpoint",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jake Mensch",
  "license": "ISC",
  "config": {
    "port": 28464,
    "endpoint": "/health"
  },
  "dependencies": {
    "pmx": "^1.5.1"
  }
}
